//test/widgets/medication_card_test.dart

/*
import 'package:dawaii/presentation/screens/home/widgets/medication_card.dart';
import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('MedicationCard displays medication info', (tester) async {
    // Arrange
    final medication = MedicationSchedule(
      scheduleId: '1',
      medicationName: 'غلوكوفاج 500',
      currentStock: 30,
      /* ... */
    );

    // Act
    await tester.pumpWidget(
      MaterialApp(
        home: Scaffold(
          body: MedicationCard(medication: medication),
        ),
      ),
    );

    // Assert
    expect(find.text('غلوكوفاج 500'), findsOneWidget);
    expect(find.text('30'), findsOneWidget);
  });
}

 */

///// ثم تنفيذ هذا الامر في الترمنل

//keytool -genkey -v -keystore ~/dawaii-keystore.jks -keyalg RSA -keysize 2048 -validity 10000 -alias dawaii